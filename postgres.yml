apiVersion: v1
kind: Service
metadata:
    name: db
    labels:
        name: db-service
        app: demo-voting-app
spec:
    ports:
        - targetPort: 5432
          port: 5432
    selector:
        name: postres-pod
        paid: demo-voting-app
