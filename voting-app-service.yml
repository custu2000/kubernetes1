apiVersion: v1
kind: Service
metadata:
    name: voting-service
    labels:
        name: voting-service
        app: demo-voting-app
spec:
    ports:
        - targetPort: 80
          port: 80
    selector:
        name: voting-app-pod
        paid: demo-voting-app
